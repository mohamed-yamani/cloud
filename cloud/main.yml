---
  - name: cloud-1.
    hosts: all
    become: true

    roles:
      - docker_install

      - docker_network
      - docker_nginx
      - nginx_conf


      # - name: copy nginx config
      #   copy:
      #     src: ./ansible.cfg
      #     dest: /
      
      # - name: copy nginx file to container
      #   shell: docker cp /ansible.cfg nginx:/etc/nginx/conf.d/ansible.cfg
          # owner: root:root

    
      
      # - name: Show the result of the echo.
      #   debug: var=echo_result.stdout

      # - name: Ensure Apache is installed.
      #   apt: 
      #     name: "{{ apache_package }}"
      #     state: present
      
      # - name: Copy the Apache config file.
      #   copy:
      #     src: test
      #     dest: "{{ apache_conf_dir }}/test.conf"
      #   notify: restart apache

      # - name: Ensure Apache is running and starts at boot.
      #   service:
      #     name: "{{ apache_service }}"
      #     state: started
      #     enabled: yes